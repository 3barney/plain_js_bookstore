// Initialize app

/**
 * Imitate namespaces,
 * Define a rook namespace(object), define other namespaces,
 * one for each part of our app (model, view, controller)
 */

var publicLibrary = {
  model: {},
  view: {},
  controller: {}
};
